{% extends "../partials/layout.njk" %}

{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}

{% set pageTitle = applicationName + " – Incentive levels in " + prisonName %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">

      <h1 class="govuk-heading-xl">Incentive levels in {{ prisonName }}</h1>

      {% set rows = [] %}

      {% for prisonIncentiveLevel in prisonIncentiveLevels %}
        {% set url -%}
          /prison-incentive-levels/{{ prisonIncentiveLevel.levelCode }}
        {%- endset %}

        {% set rows = (rows.push({
          key: { text: "Level" },
          value: { text: prisonIncentiveLevel.levelDescription },
          actions: {
            items: [
              {
                href: url,
                text: "View",
                visuallyHiddenText: "level"
              }
            ]
          }
        }), rows) %}
      {% endfor %}

      {{ govukSummaryList({
        rows: rows
      }) }}

    </div>
  </div>
{% endblock %}
